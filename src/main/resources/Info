1. ResponseEntity - это спец. класс, который представляет http-ответ.
Он содержит тело ответа, код состояния, заголовки.

2. Репозитории работают с БД(UserRepo) - первый слой абстракции

3. Сервис уже работает с какой-то конкретной логикой(UserService) - второй слой абстракции

4. Контроллер(UserController) работает с запросами и ответами - третий слой абстракции

5. @Entity - указываем данный класс такой аннотацией, который представляет собой сущность

6. @GeneratedValue(strategy = GenerationType.IDENTITY) - автоинткрементирует наш ID

7,
